<nav id="navbar">
    <!-- Navbar content goes here -->
    <a href="/">Home</a>
    <a href="/mealKits/on-the-menu">On-the-menu</a>
        
    <% if (typeof user === "undefined") { %>
        <a href="/sign-up">Sign-up</a>
        <a href="/log-in">Log-in</a>
    <% } else { %> 
        <% if (role === "customer") { %>
            <span style="color: blueviolet;">Hello <a href="/cart"><%= user.firstName %></a></span>
        <% } else { %>
            <%= role %>
            <span style="color: blueviolet;">Hello <a href="/mealKits/list"><%= user.firstName %></a></span>
        <% } %>
        <a href="/log-out">Log-out</a>
    <% } %>

    <div class="dropdown-icon">&#9776;</div> <!-- This icon will show on smaller screens -->
</nav>
